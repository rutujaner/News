import 'package:flutter/material.dart';

class ForthPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Please put some valid input!!", style: TextStyle(fontSize: 25.0)),
      ),
    );
  }
}

